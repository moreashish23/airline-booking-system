server:
  port: 5004

spring:
  application:
    name: location-service

  datasource:
    url: jdbc:mysql://localhost:3306/airline_location_service_db
    username: root
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
